/* web storage provides two objects for storing data on the client

- window.localStorage - Stores data with no expiration date
- window.sessionStorage : stores data for one session 

*/

/* localStorage- to display the localStorage object
   localStorage.clear()- to remove everything in the local storage
   localStorage.getItem(): to display data stored in the localStorage. It takes a key as a parameter.
   localStorage.setItem(): to store data in the localStorage. 
   localStorage.removeItem(): to remove stored item from a localStorage.
   localSTorage.key(): to display a data stored in a localStorage. It takes index as a parameter. 

*/

/* setting item to the localstorage

localStorage.setItem('key','value')
*/
